<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team1.executor.mapper.AccountMapper">

    <!-- AccountRow is a record: no setters, so map through its canonical constructor.
         Args are positional and must stay in component order. -->
    <resultMap id="accountResultMap" type="com.team1.executor.model.AccountRow">
        <constructor>
        <idArg column="client_id" javaType="java.lang.Long"/>
        <arg column="name" javaType="java.lang.String"/>
        <arg column="created_on" javaType="java.time.LocalDateTime"/>
        <arg column="account_state" javaType="java.lang.String"/>
        <arg column="wallet_balance" javaType="java.math.BigDecimal"/>
        <arg column="version" javaType="java.lang.Integer"/>
        <arg column="updated_on" javaType="java.time.LocalDateTime"/>
        </constructor>
    </resultMap>

    <select id="findByClientId" resultMap="accountResultMap">
        SELECT client_id, name, created_on,
               account_state, wallet_balance, version, updated_on
        FROM clients
        WHERE client_id = #{clientId}
    </select>

    <select id="findByClientIdForUpdate" resultMap="accountResultMap">
        SELECT client_id, name, created_on,
               account_state, wallet_balance, version, updated_on
        FROM clients
        WHERE client_id = #{clientId}
        FOR UPDATE
    </select>

    <update id="updateWalletBalanceGuarded">
        UPDATE clients
        SET wallet_balance = wallet_balance + #{cashDelta},
            version = version + 1,
            updated_on = now()
        WHERE client_id = #{clientId}
          AND version = #{expectedVersion}
    </update>

</mapper>